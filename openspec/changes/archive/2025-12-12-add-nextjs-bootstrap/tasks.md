## 1. Implementation
- [x] 1.1 Scaffold Next.js App Router project with TypeScript strict enabled.
- [x] 1.2 Configure `@/` path alias in `tsconfig.json` and Next config.
- [x] 1.3 Add Tailwind CSS base setup (config, globals) and Shadcn UI prerequisites.
- [x] 1.4 Install core runtime dependencies: Next.js, React/DOM, Tailwind toolchain, TanStack Query, React Flow, zod.
- [x] 1.5 Install data-layer dependencies: `drizzle-orm`, `drizzle-kit`, `better-sqlite3` and wire `serverComponentsExternalPackages` if needed.
- [x] 1.6 Add package scripts for dev, build, lint, test, and a seed stub aligning with project conventions.

## 2. Validation
- [x] 2.1 Run `npm run lint` (or `next lint`) to ensure the scaffold passes.
- [x] 2.2 Run `openspec validate add-nextjs-bootstrap --strict`.
